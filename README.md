# Labrab
Саяпин Сергей Ки25-21б Вариант 16

подключаем библиотеку System.IO, чтобы работать с файлами, дальше спрашиваем путь к файлу, пишем Console.Write с текстом «Файл: », потом Console.ReadLine читает ответ и кладем его в переменную path.
читаем файл, открываем его и отдаем все строки массивом, кладем в lines.
смотрю размеры lines.Length — сколько строк, lines[0].Length — сколько символов в первой строке, это столбцы. Кладу в rows и cols.
делаю таблицу символов — char[,] map размером rows на cols
потом двойной цикл: внешний по строкам, внутренний по столбцам, берем lines[r][c] и кладем в map[r, c],
спрашиваю координаты x и y, x - это столбец, y - это строка, если программа находит #, то меняем ее на *, если не находит, то ничего не делаем
сохраняем File.WriteAllLines(path, lines) — перезаписывает файл
